#!/bin/bash

foreground=$(xrdb -query | grep 'color0:'| awk '{print $NF}')
background=$(xrdb -query | grep 'color13:'| awk '{print $NF}')

temp="$(cat /sys/class/thermal/thermal_zone0/temp | cut -c1-2)"
echo " $temp °C "
echo

echo $foreground
echo $background
