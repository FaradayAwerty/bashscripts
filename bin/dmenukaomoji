#!/bin/bash

# set variables
[ -z $kaomoji_dir ] && kaomoji_dir=~/.local/share/dmenukaomoji/kaomoji.d
[ -z $MENU ] && MENU=dmenu


# get character using dmenu
dir=$(ls $kaomoji_dir | $MENU "$@")
[ $dir != "" ] && file=$(ls $kaomoji_dir/$dir | $MENU "$@")
[ $file != "" ] && character=$(cat $kaomoji_dir/$dir/$file | $MENU "$@")


# copy character to clipboard
echo -n $character | xclip -selection clipboard
echo -n $character | xclip -selection clipboard
echo -n $character | xclip -selection clipboard

