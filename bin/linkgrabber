#!/bin/bash

# from http://st.suckless.org/patches/externalpipe/

regex='(((http|https|ftp|gopher)|mailto)[.:][^ >"\t]*|www\.[-a-z0-9.]+)[^ .,;\t>">\):]'
url=$(grep -Po "$regex" | dmenu) || exit
dmenulink "$url"

