#!/bin/bash

succsess=true

filename=$(echo "$2" | cut -d. -f1)
compiler="$1"

$compiler "$2" -o /tmp/$filename || succsess=false

[ $succsess == true ] && clear && echo -e "\n\n\n" && /tmp/$filename
read

