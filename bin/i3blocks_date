#!/bin/bash

foreground=$(xrdb -query | grep 'color0:'| awk '{print $NF}')
background=$(xrdb -query | grep 'color4:'| awk '{print $NF}')

echo "$(date +' %b %d %a ')"
echo "$(date +' %b %d %a ')"
echo $foreground
echo $background
